replicaCount: 1

image:
  repository: koventhan/braintaskapp
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  targetPort: 3000
  port: 3000
  type: LoadBalancer

# This controls the "app" label used in the selector and service
nameOverride: "braintaskapp"