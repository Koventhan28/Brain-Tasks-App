apiVersion: v1
kind: Service
metadata:
  name: {{ include "braintaskapp.fullname" . }}
  labels:
    app: {{ include "braintaskapp.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "braintaskapp.name" . }}